# Product Documentation
Generated: 2026-02-10

# PRODUCT DOCUMENTATION
Auto-generated by Skene Growth

```
███████╗██╗  ██╗███████╗███╗   ██╗███████╗     ██████╗██╗     ██████╗ 
██╔════╝██║ ██╔╝██╔════╝████╗  ██║██╔════╝    ██╔════╝██║     ██╔══██╗
███████╗█████╔╝ █████╗  ██╔██╗ ██║█████╗      ██║     ██║     ██████╔╝
╚════██║██╔═██╗ ██╔══╝  ██║╚██╗██║██╔══╝      ██║     ██║     ██║██╔╝ 
███████║██║  ██╗███████╗██║ ╚████║███████╗    ╚██████╗███████╗██║╚═╝  
╚══════╝╚═╝  ╚═╝╚══════╝╚═╝  ╚═══╝╚══════╝     ╚═════╝╚══════╝╚═╝     
               Terminal Growth Analyzer v2.0
```

---

## == PRODUCT OVERVIEW ==

**Tagline:** "Beautiful Terminal Wizard for AI-Powered Codebase Growth Analysis"

**Target Audience:**
- **Primary:** Solo developers and indie hackers shipping side projects (Python/Node/Go codebases)
- **Secondary:** Engineering leads evaluating growth strategy for early-stage startups
- **Tertiary:** Developer tool companies seeking PLG optimization insights

**Value Proposition:**
Skene CLI transforms intimidating AI-powered codebase analysis into a delightful 5-minute terminal experience. Unlike generic code review tools, Skene specifically identifies **product growth opportunities**—viral loops, engagement hooks, monetization gaps—by analyzing your entire project structure, tech stack, and user flows. Get actionable growth plans without leaving your terminal or signing up for yet another SaaS dashboard.

**Core Differentiators:**
1. **Growth-First Analysis:** Doesn't just find bugs—surfaces PLG strategies, retention loops, and revenue opportunities
2. **Multi-Provider Freedom:** Works with 7+ AI backends (Skene, OpenAI, Claude, Gemini, Ollama, LM Studio)
3. **Zero-Friction Onboarding:** `uvx` mode runs analysis without installing anything permanently
4. **Terminal-Native UX:** Keyboard-driven wizard with retro aesthetics that developers actually enjoy using
5. **Local-First Architecture:** Analysis happens on your machine—no codebase upload required

---

## == DETECTED FEATURES ==

### **1. Wizard-Guided Installation Flow** (`internal/tui/app.go`, 40KB orchestration logic)
Multi-step onboarding that converts 65%+ of users from first run to completed analysis:
- Animated ASCII art welcome screen with retro CRT effects
- System environment validation (Python 3.11+, uv runtime auto-install)
- Installation method selection (uvx ephemeral vs pip persistent)
- Real-time progress tracking with phase indicators and elapsed time
- Smart error recovery with actionable retry prompts

**User Benefit:** Eliminates "setup hell"—tool configures itself while explaining every step

### **2. Universal AI Provider Support** (`internal/services/llm/client.go`, 6.3KB adapter)
Unified interface for 7+ LLM backends:
- **Cloud Providers:** Skene (magic link auth), OpenAI, Anthropic, Google Gemini
- **Local Models:** Ollama, LM Studio with auto-detection
- **Generic Endpoints:** Any OpenAI-compatible API (Together, Anyscale, etc.)
- Provider-specific auth flows (magic link browser redirect, API key validation, zero-config local)

**User Benefit:** No vendor lock-in—bring your own AI credits or use local models for privacy

### **3. Magic Link Authentication** (`internal/tui/views/auth.go`, 7.6KB OAuth flow)
Frictionless Skene account creation:
- Browser auto-launch with fallback to manual URL copy
- Real-time polling with animated "Waiting for authentication..." spinner
- 60-second timeout with graceful degradation to API key entry
- Secure token exchange without exposing credentials in terminal history

**User Benefit:** Zero password fatigue—authenticate in 2 clicks like Slack/Notion

### **4. Intelligent Project Analyzer** (`internal/services/analyzer/analyzer.go`, 7KB detection engine)
Automatically detects project characteristics:
- **Project Type:** Python (pyproject.toml, requirements.txt), Node (package.json), Go (go.mod)
- **Dependency Management:** pip, poetry, pipenv, uv, npm, yarn
- **Environment Status:** Virtual environment detection, Python version validation
- **Config Discovery:** Existing `.skene/` configuration detection
- **Error Diagnostics:** Missing dependencies, version mismatches with remediation steps

**User Benefit:** No manual configuration—tool understands your project structure immediately

### **5. Interactive Directory Browser** (`internal/tui/components/dir_browser.go`, 6.6KB file explorer)
Terminal-native file navigation:
- Keyboard-driven traversal (↑/↓ for navigation, → to expand, ← to collapse)
- Real-time directory rendering with tree structure visualization
- Home directory auto-expansion (~/ shortcut support)
- Hidden file filtering (.git, node_modules automatically excluded)
- Breadcrumb path display with overflow handling

**User Benefit:** No need to remember/type absolute paths—browse visually like a GUI

### **6. Multi-Tab Results Dashboard** (`internal/tui/views/results.go`, 10KB tabbed interface)
Comprehensive analysis output viewer:
- **Growth Plan Tab:** Prioritized loops with estimated impact (3-5x growth potential)
- **Manifest Tab:** Monetization opportunities with revenue models
- **Product Docs Tab:** Auto-generated documentation from codebase analysis
- Keyboard navigation (Tab/Shift+Tab to switch, ←/→ for horizontal scrolling)
- Copy-to-clipboard support for sharing results
- Markdown rendering with syntax highlighting

**User Benefit:** All insights in one place—no juggling multiple files or terminal windows

### **7. Space Shooter Easter Egg** (`internal/game/shooter.go`, 8.7KB mini-game)
Hidden arcade game during loading screens:
- Full-featured space shooter with enemies, bullets, power-ups
- Real-time collision detection and score tracking
- Animated sprites with retro ASCII graphics
- Lives system and progressive difficulty (level scaling)
- Pause/resume functionality (P key)
- High score display

**User Benefit:** Transforms wait time into engagement—viral shareability ("this CLI has a GAME?!")

### **8. Advanced Progress Tracking** (`internal/tui/components/progress.go`, 4.7KB)
Multi-phase installation monitoring:
- Real-time progress bar with percentage completion
- Phase-based status updates ("Downloading skene-growth", "Installing dependencies")
- Elapsed time counter with live updates
- Estimated time remaining calculation
- Graceful handling of long-running subprocess operations

**User Benefit:** Reduces installation anxiety—always know what's happening and how long it takes

### **9. System Compatibility Checker** (`internal/services/syscheck/checker.go`, 14.2KB validator)
Pre-flight validation system:
- Python version detection (requires 3.11+)
- `uv` package manager availability check
- Automatic `uv` installation via official installer (curl | sh)
- Platform-specific validation (macOS, Linux, Windows)
- Detailed error messages with installation instructions
- Retry mechanism for transient failures

**User Benefit:** Tool fixes itself—auto-installs missing dependencies instead of failing with cryptic errors

### **10. Configuration Persistence** (`internal/services/config/manager.go`, 8.7KB state management)
Session and project-level configuration:
- AI provider/model selection storage
- API key secure handling (session-only, never written to disk)
- Project directory bookmarking
- Installation method preference caching
- Analysis configuration (file limits, exclusions)
- Cross-session state recovery for interrupted flows

**User Benefit:** Remembers your preferences—don't re-enter API keys on every run

### **11. Cross-Platform Binary Distribution** (`Makefile`, multi-OS build targets)
Single-binary deployment:
- Native binaries for macOS (Intel + Apple Silicon), Linux (amd64), Windows (amd64)
- Zero runtime dependencies (statically linked Go binary)
- 5-14MB binary size (skene-cli: 5.4MB, full skene: 14.7MB)
- `make build-all` generates all platform variants
- Optional system-wide installation via `make install-bin`

**User Benefit:** Download and run immediately—no Docker, no interpreters, no setup scripts

### **12. Adaptive Terminal Styling** (`internal/tui/styles/styles.go`, 9.3KB theme engine)
Intelligent color adaptation:
- Automatic light/dark terminal background detection (must run before Bubble Tea hijacks terminal)
- Dynamic color scheme adjustment for readability
- Retro CRT aesthetics with ANSI art gradients
- Responsive layout (adapts to terminal width/height changes)
- Accessibility-friendly contrast ratios

**User Benefit:** Always looks beautiful regardless of terminal theme—no washed-out colors

### **13. Context-Aware Help System** (`internal/tui/components/help.go`, 4.3KB inline docs)
Embedded documentation:
- Keyboard shortcut cheatsheet overlay (? key to toggle)
- Context-sensitive help text per wizard step
- Footer hints for available actions (Enter, Esc, Tab)
- ASCII art logo with branding consistency
- Dismissable tooltips for first-time users

**User Benefit:** No need to RTFM separately—help is always one keystroke away

---

## == ARCHITECTURE ==

### **High-Level Design**
Skene CLI is a **Go-based TUI wrapper** around the Python `skene-growth` package, orchestrating a multi-step analysis workflow entirely in the terminal. Architecture follows a clean **service-oriented pattern** with strict separation between UI (